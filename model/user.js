class User {
  constructor(userId, firstName, lastName, email, photo) {
    this.userId = userId;
    this.firstName= firstName;
    this.lastName = lastName;
    this.email = email;
    this.photo = photo;
  }
}
module.exports = User;
